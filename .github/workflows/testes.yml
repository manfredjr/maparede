# CI do MapaRede - MT: compila, roda os testes e gera o .exe em todo Pull Request
# e em todo push no main. O .exe fica disponivel para baixar na execucao, em Artifacts.
#
# Quando uma marca de versao (v0.1.0, por exemplo) chega ao GitHub, o mesmo
# fluxo publica a Release com o maparede.exe anexado. E dessa Release que a
# pagina maparede.manfred.com.br baixa o programa.
name: testes

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:

permissions:
  contents: read

jobs:
  testes:
    runs-on: windows-latest
    permissions:
      contents: write
    env:
      DOTNET_NOLOGO: 1
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Compilar
        if: hashFiles('maparede.sln') != ''
        run: dotnet build maparede.sln -c Release

      - name: Testar
        if: hashFiles('maparede.sln') != ''
        run: dotnet test maparede.sln -c Release --no-build

      - name: Gerar o .exe
        if: hashFiles('maparede.sln') != ''
        run: dotnet publish src/maparede/maparede.csproj -c Release -o publicar

      # O .sha256.txt deixa quem baixou conferir que o .exe chegou igual ao publicado.
      - name: Calcular o SHA-256
        if: hashFiles('maparede.sln') != ''
        shell: pwsh
        run: |
          $hash = (Get-FileHash publicar/maparede.exe -Algorithm SHA256).Hash
          Set-Content -Path publicar/maparede.exe.sha256.txt -Value "$hash  maparede.exe" -Encoding ascii
          Get-Content publicar/maparede.exe.sha256.txt

      - name: Guardar o .exe
        if: hashFiles('maparede.sln') != ''
        uses: actions/upload-artifact@v4
        with:
          name: maparede-${{ github.sha }}
          path: |
            publicar/maparede.exe
            publicar/maparede.exe.sha256.txt
          retention-days: 14

      - name: Conferir a versao da marca
        if: startsWith(github.ref, 'refs/tags/v')
        shell: pwsh
        run: |
          $versao = ([xml](Get-Content Directory.Build.props)).Project.PropertyGroup[0].Version
          $marca = "${{ github.ref_name }}".TrimStart('v')
          if ($versao -ne $marca) {
            throw "A marca $marca nao bate com a versao $versao do Directory.Build.props."
          }

      - name: Publicar a Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          name: MapaRede - MT ${{ github.ref_name }}
          files: |
            publicar/maparede.exe
            publicar/maparede.exe.sha256.txt
          generate_release_notes: true
